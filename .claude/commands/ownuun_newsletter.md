# Ownuun Newsletter

뉴스레터를 생성하고, 사용자 검토 후 발송합니다.

ARGUMENTS: (선택) 기간 - 기본값 7일

## 워크플로우

```
Phase 1: 뉴스레터 생성
    │
Phase 2: 미리보기 출력 → 사용자 검토
    │
Phase 3: 수정 요청 처리 (있으면)
    │
Phase 4: 승인 → 테스트 발송 → 전체 발송
```

---

## Phase 1: 뉴스레터 생성

### Step 1.1: 최근 콘텐츠 조회

```bash
cd /Users/ownuun/conductor/workspaces/v2-v1/kiev && node -e "
const { createClient } = require('@supabase/supabase-js');
require('dotenv').config({ path: '.env.local' });
const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY);

async function getRecentContent() {
  const days = 7;
  const startDate = new Date();
  startDate.setDate(startDate.getDate() - days);

  const { data, error } = await supabase
    .from('content')
    .select('id, title, title_en, description, body, type, external_url, view_count, published_at')
    .eq('status', 'published')
    .gte('published_at', startDate.toISOString())
    .order('view_count', { ascending: false })
    .limit(30);

  if (error) {
    console.error('Error:', error.message);
    return;
  }

  console.log('=== 최근 ' + days + '일간 콘텐츠 (조회수 순, ' + data.length + '개) ===');
  data.forEach((item, i) => {
    const platform = item.type;
    const views = item.view_count || 0;
    console.log((i+1) + '. [' + views + '회] ' + platform.padEnd(12) + ' ' + item.title);
    console.log('   ' + (item.description?.slice(0, 80) || '') + '...');
    console.log();
  });

  // 구독자 수 확인
  const { count } = await supabase
    .from('newsletter_subscribers')
    .select('*', { count: 'exact', head: true })
    .eq('is_active', true);

  console.log('\\n구독자 수: ' + (count || 0) + '명');
}
getRecentContent();
"
```

### Step 1.2: AI로 뉴스레터 생성

콘텐츠를 분석하여 뉴스레터를 생성합니다.

**뉴스레터 구조:**

```json
{
  "greeting": "새해 첫 AI 트렌드입니다!",
  "intro": "이번 주는 특히 에이전트 관련 소식이 많았습니다. 주요 하이라이트를 정리했습니다.",
  "sections": [
    {
      "emoji": "🔥",
      "title": "이번 주 하이라이트",
      "items": [
        {
          "id": "uuid",
          "title": "제목",
          "summary": "2-3문장 요약",
          "url": "https://...",
          "platform": "youtube",
          "thumbnailUrl": "https://... (썸네일 이미지 URL, 있으면 포함)"
        }
      ]
    },
    {
      "emoji": "📺",
      "title": "추천 영상",
      "items": [...]
    },
    {
      "emoji": "🛠️",
      "title": "오픈소스 픽",
      "items": [...]
    },
    {
      "emoji": "💡",
      "title": "인사이트",
      "items": [...]
    }
  ]
}
```

**생성 가이드라인:**

- 섹션은 3-4개로 구성
- 각 섹션당 2-4개 아이템 선정
- 추천점수 7점 이상 위주로 선정
- 다양한 플랫폼 믹스 (YouTube, GitHub, X, Reddit 등)
- 요약은 독자가 클릭하고 싶게 작성
- **썸네일 이미지**: 콘텐츠에 `thumbnailUrl`이 있으면 그대로 포함 (YouTube 영상, GitHub 레포 등)

---

## Phase 2: 미리보기 출력

생성된 뉴스레터를 텍스트로 미리보기:

```
════════════════════════════════════════════════════════════
📬 AI Community 뉴스레터 미리보기
════════════════════════════════════════════════════════════

{greeting}

{intro}

────────────────────────────────────────────────────────────
🔥 이번 주 하이라이트
────────────────────────────────────────────────────────────

1. [YOUTUBE] {제목}
   {2-3문장 요약}
   🔗 {URL}

2. [GITHUB] {제목}
   {2-3문장 요약}
   🔗 {URL}

────────────────────────────────────────────────────────────
📺 추천 영상
────────────────────────────────────────────────────────────

1. [YOUTUBE] {제목}
   {2-3문장 요약}
   🔗 {URL}

... (섹션 반복)

════════════════════════════════════════════════════════════
📊 메타데이터
- 총 아이템: N개
- 구독자: N명
- 발송 예정: 전체 구독자
════════════════════════════════════════════════════════════
```

---

## Phase 3: 사용자 검토

미리보기 출력 후 사용자에게 질문:

```
뉴스레터 검토 결과를 알려주세요:

1. ✅ 승인 - 이대로 발송
2. ✏️ 수정 요청 - 특정 내용 변경 (예: "3번 아이템 제거", "인사말 변경")
3. ❌ 취소 - 발송하지 않음

선택:
```

**수정 요청 처리:**

- "N번 아이템 제거" → 해당 아이템 삭제
- "섹션 순서 변경" → 섹션 재배열
- "인사말/인트로 수정" → 텍스트 교체
- "요약 수정" → 특정 아이템 요약 재작성

수정 후 다시 미리보기 출력 → 재검토

---

## Phase 4: 발송

### Step 4.1: 테스트 발송 (권장)

```
테스트 이메일 주소를 입력하세요 (또는 Enter로 스킵하여 바로 전체 발송):
>
```

**테스트 발송 명령어:**

```bash
cd /Users/ownuun/conductor/workspaces/v2-v1/kiev && curl -X POST http://localhost:3000/api/newsletter/send \
  -H "Content-Type: application/json" \
  -d '{
    "newsletter": <NEWSLETTER_JSON>,
    "issueNumber": <ISSUE_NUMBER>,
    "testEmail": "<TEST_EMAIL>"
  }'
```

테스트 발송 후:

```
테스트 이메일이 발송되었습니다. 확인 후 전체 발송을 진행할까요?

1. ✅ 전체 발송
2. ❌ 취소

선택:
```

### Step 4.2: 전체 발송

```bash
cd /Users/ownuun/conductor/workspaces/v2-v1/kiev && curl -X POST http://localhost:3000/api/newsletter/send \
  -H "Content-Type: application/json" \
  -d '{
    "newsletter": <NEWSLETTER_JSON>,
    "issueNumber": <ISSUE_NUMBER>
  }'
```

**발송 결과:**

```
════════════════════════════════════════════════════════════
✅ 뉴스레터 발송 완료!
════════════════════════════════════════════════════════════

📬 발송 결과:
- 성공: N명
- 실패: N명
- 총 구독자: N명

📅 발송 시간: 2025-01-20 11:30:00 KST
📋 호수: #N

다음 발송까지 수고하셨습니다! 🎉
════════════════════════════════════════════════════════════
```

---

## 이슈 번호 관리

이전 발송 기록을 확인하여 이슈 번호 자동 증가:

```bash
cd /Users/ownuun/conductor/workspaces/v2-v1/kiev && curl http://localhost:3000/api/newsletter/history
```

마지막 이슈 번호 + 1 = 이번 이슈 번호

---

## 실행 예시

```
/ownuun_newsletter
```

또는 기간 지정:

```
/ownuun_newsletter 14
```

(최근 14일간 콘텐츠 기준)
